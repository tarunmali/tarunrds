import React from 'react';

function Cp(props) {
    return (
        <div>
            <h1>Cp questions submission  support will be added soon with online judge and submitted code saving feature </h1>
            <h3>{props.question}</h3>
        </div>
    );
}

export default Cp;